import { Module } from '@nestjs/common';

/**
 * `contratos` — `contrato` (1 por `(pessoa, produto)`, perpétuo), `aditivo`,
 * o fold de recálculo puro e o estado de `acesso`. Vazio na spec 001; preenchido
 * na spec 025 (contratos-aditivos-fold).
 */
@Module({})
export class ContratosModule {}
