import { Module } from '@nestjs/common';

/**
 * `clientes` — `pessoa`, `conta`, engine de identidade/dedup e `merge`. Vazio na
 * spec 001; preenchido na spec 005 (pessoa-identidade-dedup).
 */
@Module({})
export class ClientesModule {}
