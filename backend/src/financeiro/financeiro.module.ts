import { Module } from '@nestjs/common';

/**
 * `financeiro` — `transacao` normalizada, vínculo Asaas↔Guru, receita (queries)
 * e reconciliação. Vazio na spec 001; preenchido a partir da spec 018
 * (financeiro-transacao-ledger).
 */
@Module({})
export class FinanceiroModule {}
