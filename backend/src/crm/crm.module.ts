import { Module } from '@nestjs/common';

/**
 * `crm` — `interacao`, `oportunidade`, `pipeline`, `tarefa`, `nota`, `tag`,
 * `lead`, disparos, workflow e FAQ. Vazio na spec 001; preenchido a partir da
 * spec 007 (crm-administracao) — prioridade 1 do dono do produto.
 */
@Module({})
export class CrmModule {}
