import { Module } from '@nestjs/common';

/**
 * `marketing` — `campanha`, `artefato`, `versao_campo` (diff imutável), `lead`,
 * `tratamento_cliente`, `atribuicao`. Vazio na spec 001; preenchido a partir da
 * spec 032 (marketing-campanha-e-versionamento).
 */
@Module({})
export class MarketingModule {}
